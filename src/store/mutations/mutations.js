export default {
	trending(state, items) {
		state.trending = items
	},

	myList(state, items) {
		state.myList = items
	},

	films(state, items) {
		state.films = items
	},

	shows(state, items) {
		state.shows = items
	},

	genres(state, items) {
		state.genres = items
	}
}